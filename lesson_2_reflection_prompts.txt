What happens when you initialize a repository? Why do you need to do it?
A .git file is created. It is needed to track the history of the files in directory. It is the very purpose of repository.	

How is the staging area different from the working directory and the repository?
What value do you think it offers?
A staging area is a connection between working directory and the repository. It allows us to commit per logical change.

How can you use the staging area to make sure you have one commit per logical
change?
If we want to add more than one changes in a single commit we can add them into staging area and then commit them at once.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?
Branches can be added when we are trying an experimental version or a different language version.

How do the diagrams help you visualize the branch structure?
The diagrams are a good way to keep track of the emerging branches and the commits.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?
Merging helps convert two branches into one. 

What are the pros and cons of Git’s automatic merging vs. always doing merges
manually?
It saves lot 0f time and hard word while it can lead to conflict sometimes.
